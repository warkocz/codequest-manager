- if @order.persisted?
  h1 Edit order
- else
  h1 New order
= form_for @order do |f|
  - selected = @order.user_id if @order.persisted?
  = f.select :user_id, options_for_select(@users, selected)
  = f.text_field :from, placeholder: 'Where are we ordering from?'
  = f.submit class: 'button'